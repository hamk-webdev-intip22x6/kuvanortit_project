from django.db import models

"""
class User (models.Model):
    name = models.CharField(max_length=20)
    information = models.CharField(max_length=1000)
    date_joined = models.DateTimeField('date created', auto_now_add=True)
"""
